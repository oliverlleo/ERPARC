name: Apply ERP fixes

on:
  push:
    branches:
      - fix/financial-consistency-firestore-performance
    paths:
      - .github/workflows/apply-erp-fixes.yml

permissions:
  contents: write

jobs:
  apply-fixes:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply guarded patches
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new, label):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected exactly 1 match in {path}, found {count}')
              p.write_text(text.replace(old, new, 1), encoding='utf-8')
              print(f'OK: {label}')

          def replace_exact_count(path, old, new, expected, label):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              count = text.count(old)
              if count != expected:
                  raise SystemExit(f'{label}: expected {expected} matches in {path}, found {count}')
              p.write_text(text.replace(old, new), encoding='utf-8')
              print(f'OK: {label} ({count} replacements)')

          # 1) Cash-flow reads: keep parent snapshots attached to transaction docs so
          # enrichAndUnifyTransactions does not re-read every parent document.
          replace_once(
              'fluxo-de-caixa.js',
              '''    async function fetchTransactionsEfficiently(parentCollectionName, subcollectionName, startDate, endDate, inclusive = true) {
                  // This approach is more efficient as it pre-filters parent documents by a relevant date field.
                  // This reduces the number of subcollection queries needed.
                  const parentDateFilterField = parentCollectionName === 'despesas' ? 'vencimento' : 'dataVencimento';

                  // Create a broader query on the parent collection.
                  // We fetch parents from a wider date range to catch transactions that might have been paid/received
                  // outside their due date but still fall within our cash flow period.
                  let parentQuery = collection(db, `users/${userId}/${parentCollectionName}`);

                  // The query for subcollections will be precise, so the parent query can be broader.
                  // This is a balance between performance and correctness.
                  // For simplicity in this fix, we'll still fetch all parents, but the sub-query will be precise.
                  // A more advanced optimization could pre-filter parents by date.

                  const parentDocsSnapshot = await getDocs(parentQuery);

                  const promises = parentDocsSnapshot.docs.map(parentDoc => {
                      let subcollectionQuery = collection(parentDoc.ref, subcollectionName);

                      // Apply the precise date filtering at the subcollection level.
                      if (startDate) {
                           subcollectionQuery = query(subcollectionQuery, where('dataTransacao', inclusive ? '>=' : '<', startDate));
                      }
                      if (endDate) {
                           subcollectionQuery = query(subcollectionQuery, where('dataTransacao', inclusive ? '<=' : '<', endDate));
                      }

                      return getDocs(subcollectionQuery);
                  });

                  const querySnapshots = await Promise.all(promises);
                  return querySnapshots.flatMap(snapshot => snapshot.docs);
              }
''',
              '''    async function fetchTransactionsEfficiently(parentCollectionName, subcollectionName, startDate, endDate, inclusive = true) {
                  const parentCollectionRef = collection(db, `users/${userId}/${parentCollectionName}`);
                  const parentDocsSnapshot = await getDocs(parentCollectionRef);

                  // Keep each already-loaded parent together with its transaction docs. This avoids
                  // an additional getDoc(parent) for every payment/receipt during enrichment while
                  // preserving the existing query semantics for transaction dates.
                  const transactionGroups = await Promise.all(parentDocsSnapshot.docs.map(async parentDoc => {
                      let subcollectionQuery = collection(parentDoc.ref, subcollectionName);

                      if (startDate) {
                          subcollectionQuery = query(subcollectionQuery, where('dataTransacao', inclusive ? '>=' : '<', startDate));
                      }
                      if (endDate) {
                          subcollectionQuery = query(subcollectionQuery, where('dataTransacao', inclusive ? '<=' : '<', endDate));
                      }

                      const transactionSnapshot = await getDocs(subcollectionQuery);
                      return transactionSnapshot.docs.map(transactionDoc => ({ transactionDoc, parentDoc }));
                  }));

                  return transactionGroups.flat();
              }
''',
              'reuse parent snapshots in cash flow'
          )

          # 2) Projected transactions: bound Firestore reads by due date instead of loading
          # entire collections. Single-field range queries use Firestore automatic indexes.
          replace_once(
              'fluxo-de-caixa.js',
              '''        // Fetch ALL pending expenses and filter in code to avoid composite indexes
                  const despesasQuery = collection(db, `users/${userId}/despesas`);
                  const despesasSnap = await getDocs(despesasQuery);
''',
              '''        // Bound projected expenses by due date in Firestore, then keep status filtering
                  // client-side. This avoids full collection scans without requiring a composite index.
                  const despesasRef = collection(db, `users/${userId}/despesas`);
                  const despesasQuery = query(
                      despesasRef,
                      where('vencimento', '>=', startDate),
                      where('vencimento', '<=', endDate)
                  );
                  const despesasSnap = await getDocs(despesasQuery);
''',
              'date-bound projected expenses'
          )

          replace_once(
              'fluxo-de-caixa.js',
              '''        // Fetch ALL pending revenues and filter in code
                  const receitasQuery = collection(db, `users/${userId}/receitas`);
                  const receitasSnap = await getDocs(receitasQuery);
                  receitasSnap.forEach(doc => {
''',
              '''        // Support both the current dataVencimento field and legacy vencimento while
                  // limiting reads to the selected period. Dedupe documents that contain both fields.
                  const receitasRef = collection(db, `users/${userId}/receitas`);
                  const [receitasPorDataVencimentoSnap, receitasPorVencimentoSnap] = await Promise.all([
                      getDocs(query(
                          receitasRef,
                          where('dataVencimento', '>=', startDate),
                          where('dataVencimento', '<=', endDate)
                      )),
                      getDocs(query(
                          receitasRef,
                          where('vencimento', '>=', startDate),
                          where('vencimento', '<=', endDate)
                      ))
                  ]);
                  const receitasPorId = new Map();
                  receitasPorDataVencimentoSnap.forEach(doc => receitasPorId.set(doc.id, doc));
                  receitasPorVencimentoSnap.forEach(doc => receitasPorId.set(doc.id, doc));

                  receitasPorId.forEach(doc => {
''',
              'date-bound projected receivables with legacy support'
          )

          # 3) Enrichment: use attached parent snapshot with a compatibility fallback for any
          # caller that still supplies raw Firestore transaction documents.
          replace_once(
              'fluxo-de-caixa.js',
              '''        for (const doc of pagamentos) {
                  const data = doc.data();
''',
              '''        for (const entry of pagamentos) {
                  const doc = entry.transactionDoc || entry;
                  const data = doc.data();
''',
              'normalize payment transaction entry'
          )
          replace_once(
              'fluxo-de-caixa.js',
              '''            const parentDespesaRef = doc.ref.parent.parent;
                  if (parentDespesaRef) {
                      const despesaSnap = await getDoc(parentDespesaRef);
                      if (despesaSnap.exists()) {
''',
              '''            const parentDespesaRef = entry.parentDoc?.ref || doc.ref.parent.parent;
                  if (parentDespesaRef) {
                      const despesaSnap = entry.parentDoc || await getDoc(parentDespesaRef);
                      if (despesaSnap.exists()) {
''',
              'reuse expense parent during enrichment'
          )
          replace_once(
              'fluxo-de-caixa.js',
              '''        for (const doc of recebimentos) {
                  const data = doc.data();
''',
              '''        for (const entry of recebimentos) {
                  const doc = entry.transactionDoc || entry;
                  const data = doc.data();
''',
              'normalize receipt transaction entry'
          )
          replace_once(
              'fluxo-de-caixa.js',
              '''            const parentReceitaRef = doc.ref.parent.parent;
                   if (parentReceitaRef) {
                      const receitaSnap = await getDoc(parentReceitaRef);
                       if (receitaSnap.exists()) {
''',
              '''            const parentReceitaRef = entry.parentDoc?.ref || doc.ref.parent.parent;
                   if (parentReceitaRef) {
                      const receitaSnap = entry.parentDoc || await getDoc(parentReceitaRef);
                       if (receitaSnap.exists()) {
''',
              'reuse receivable parent during enrichment'
          )

          # 4) What-If: simulated and comparison impacts must remain cumulative after the day
          # on which they occur.
          replace_once(
              'fluxo-de-caixa.js',
              '''        let runningSaldoRealizado = saldoAnterior;
                  let runningSaldoProjetado = saldoAnterior;
                  let runningSaldoSimulado = includeProjections ? saldoAnterior : saldoAnterior; // Initialize based on projection inclusion
                  let runningSaldoComparado = includeProjections ? saldoAnterior : saldoAnterior; // Initialize based on projection inclusion


                  sortedDays.forEach(day => {
                      labels.push(new Date(day + 'T00:00:00').toLocaleDateString('pt-BR'));
                      const changes = dailyChanges[day];

                      // Update the cumulative balances correctly
                      runningSaldoRealizado += changes.realizado;
                      runningSaldoProjetado += changes.realizado + changes.projetado;

                      if (includeProjections) {
                          runningSaldoSimulado = runningSaldoProjetado + changes.simulado;
                          runningSaldoComparado = runningSaldoProjetado + changes.comparado;
                      } else {
                          runningSaldoSimulado = runningSaldoRealizado + changes.simulado;
                          runningSaldoComparado = runningSaldoRealizado + changes.comparado;
                      }


                      realizadoData.push(runningSaldoRealizado / 100);
''',
              '''        let runningSaldoRealizado = saldoAnterior;
                  let runningSaldoProjetado = saldoAnterior;
                  let runningSaldoSimulado = saldoAnterior;
                  let runningSaldoComparado = saldoAnterior;
                  let cumulativeSimulado = 0;
                  let cumulativeComparado = 0;

                  sortedDays.forEach(day => {
                      labels.push(new Date(day + 'T00:00:00').toLocaleDateString('pt-BR'));
                      const changes = dailyChanges[day];

                      runningSaldoRealizado += changes.realizado;
                      runningSaldoProjetado += changes.realizado + changes.projetado;
                      cumulativeSimulado += changes.simulado;
                      cumulativeComparado += changes.comparado;

                      // A simulated event changes every subsequent balance, not only the day on
                      // which it occurs. Use the selected base and add the cumulative scenario delta.
                      const scenarioBase = includeProjections ? runningSaldoProjetado : runningSaldoRealizado;
                      runningSaldoSimulado = scenarioBase + cumulativeSimulado;
                      runningSaldoComparado = scenarioBase + cumulativeComparado;

                      realizadoData.push(runningSaldoRealizado / 100);
''',
              'make What-If impacts cumulative'
          )

          # 5) Reconciliation audit: persist the value of currentUserName, not a literal string.
          replace_once(
              'movimentacao-bancaria.js',
              '''                usuarioConciliacao: conciliar ? "currentUserName" : null // Replace with actual user name
''',
              '''                usuarioConciliacao: conciliar ? currentUserName : null
''',
              'record actual reconciliation user'
          )

          # 6) Notifications: overdue state is derived from due date + open status, matching the UI.
          replace_once(
              'notifications.js',
              '''    // Query 2: For overdue items
              const qOverdue = query(despesasRef, where("status", "==", "Vencido"));
              const overdueSnapshot = await getDocs(qOverdue);

              overdueSnapshot.forEach(doc => {
                  const despesa = doc.data();
                  const valor = despesa.valorSaldo ?? despesa.valorOriginal ?? 0;
''',
              '''    // Query 2: Derive overdue status from due date, matching the UI logic. Keep
              // status filtering client-side so pending and partially paid titles are included.
              const qOverdue = query(despesasRef, where("vencimento", "<", todayStr));
              const overdueSnapshot = await getDocs(qOverdue);
              const overdueStatuses = new Set(["Pendente", "Vencido", "Pago Parcialmente"]);

              overdueSnapshot.forEach(doc => {
                  const despesa = doc.data();
                  const status = despesa.status || "Pendente";
                  if (!overdueStatuses.has(status)) return;

                  const valor = despesa.valorSaldo ?? despesa.valorOriginal ?? 0;
''',
              'derive overdue payables from due date'
          )

          replace_once(
              'notifications.js',
              '''    // Query 2: For overdue items
              const qOverdue = query(receitasRef, where("status", "==", "Vencido"));
              const overdueSnapshot = await getDocs(qOverdue);

              overdueSnapshot.forEach(doc => {
                  const receita = doc.data();
                  createNotification(db, userId, {
''',
              '''    // Query 2: Derive overdue status from due date. Query both current and legacy
              // due-date fields and dedupe the results to preserve compatibility with existing data.
              const [overdueByDataVencimento, overdueByVencimento] = await Promise.all([
                  getDocs(query(receitasRef, where("dataVencimento", "<", todayStr))),
                  getDocs(query(receitasRef, where("vencimento", "<", todayStr)))
              ]);
              const overdueReceitas = new Map();
              overdueByDataVencimento.forEach(doc => overdueReceitas.set(doc.id, doc));
              overdueByVencimento.forEach(doc => overdueReceitas.set(doc.id, doc));
              const overdueStatuses = new Set(["Pendente", "Vencido", "Recebido Parcialmente"]);

              overdueReceitas.forEach(doc => {
                  const receita = doc.data();
                  const status = receita.status || "Pendente";
                  if (!overdueStatuses.has(status)) return;

                  createNotification(db, userId, {
''',
              'derive overdue receivables from due date'
          )

          # 7) Use the canonical systemUsers collection for both access and employee dropdowns.
          replace_once(
              'index.html',
              '''            // Popula o dropdown na aba de criação de acessos
              const acessoUsuarioSelect = document.getElementById('acesso-usuario-select');
              populateDropdown(acessoUsuarioSelect, systemUsersDocs);

              // Renderiza a tabela de usuários do sistema
''',
              '''            // Populate every control that references a system user from the same
              // canonical collection. This prevents employee records from reading a stale/legacy
              // `usuarios` collection while access management uses `systemUsers`.
              const acessoUsuarioSelect = document.getElementById('acesso-usuario-select');
              const funcionarioUsuarioSelect = document.getElementById('funcionario-usuario-sistema');
              populateDropdown(acessoUsuarioSelect, systemUsersDocs);
              populateDropdown(funcionarioUsuarioSelect, systemUsersDocs);

              // Renderiza a tabela de usuários do sistema
''',
              'populate employee user dropdown from systemUsers'
          )

          replace_once(
              'index.html',
              '''    // Populate user dropdown in employee form
              const usuariosQueryForDropdown = query(collection(db, 'users', userId, 'usuarios'));
              onSnapshot(usuariosQueryForDropdown, (querySnapshot) => {
                  const userSelect = document.getElementById('funcionario-usuario-sistema');
                  if (userSelect) {
                      populateDropdown(userSelect, querySnapshot.docs);
                  }
                  });

''',
              '''''',
              'remove legacy usuarios listener'
          )

          # 8) Dashboard: debounce bursts of top-level snapshots and reuse the already loaded
          # expense/receivable snapshots. This removes duplicate full scans during normal use.
          replace_once(
              'index.html',
              '''    let allDespesas = []; // Holds all expenses for client-side filtering
              let allReceitas = []; // Holds all receivables for client-side filtering
''',
              '''    let allDespesas = []; // Holds all expenses for client-side filtering
              let allReceitas = []; // Holds all receivables for client-side filtering
              let despesasSnapshotLoaded = false;
              let receitasSnapshotLoaded = false;
''',
              'track cached financial snapshots'
          )

          replace_once(
              'index.html',
              '''    function setupListeners(userId) {
''',
              '''    let dashboardRefreshTimer = null;

              function scheduleDashboardRefresh() {
                  if (dashboardRefreshTimer) {
                      clearTimeout(dashboardRefreshTimer);
                  }
                  dashboardRefreshTimer = setTimeout(() => {
                      dashboardRefreshTimer = null;
                      updateAllDashboardViews();
                  }, 75);
              }

              function setupListeners(userId) {
''',
              'add dashboard refresh scheduler'
          )

          replace_once(
              'index.html',
              '''            }
              updateAllDashboardViews();
          });

          if (contasBancariasTableBody && !contasBancariasTableBody.dataset.listenerAttached) {
''',
              '''            }
              scheduleDashboardRefresh();
          });

          if (contasBancariasTableBody && !contasBancariasTableBody.dataset.listenerAttached) {
''',
              'debounce bank snapshot dashboard refresh'
          )

          replace_once(
              'index.html',
              '''        despesasUnsubscribe = onSnapshot(despesasQuery, (querySnapshot) => {
              allDespesas = querySnapshot.docs; // Store all documents for filtering
              applyFiltersAndRender(); // Render the table on the Contas a Pagar page
              updateAllDashboardViews(); // Update all dashboard cards across all views
          });
''',
              '''        despesasUnsubscribe = onSnapshot(despesasQuery, (querySnapshot) => {
              allDespesas = querySnapshot.docs; // Store all documents for filtering
              despesasSnapshotLoaded = true;
              applyFiltersAndRender(); // Render the table on the Contas a Pagar page
              scheduleDashboardRefresh(); // Coalesce simultaneous financial snapshot updates
          });
''',
              'cache payables snapshot and debounce refresh'
          )

          replace_once(
              'index.html',
              '''        receitasUnsubscribe = onSnapshot(receitasQuery, (querySnapshot) => {
              allReceitas = querySnapshot.docs;
              applyReceitasFiltersAndRender();
              updateAllDashboardViews(); // Update all dashboard cards across all views
          });
''',
              '''        receitasUnsubscribe = onSnapshot(receitasQuery, (querySnapshot) => {
              allReceitas = querySnapshot.docs;
              receitasSnapshotLoaded = true;
              applyReceitasFiltersAndRender();
              scheduleDashboardRefresh(); // Coalesce simultaneous financial snapshot updates
          });
''',
              'cache receivables snapshot and debounce refresh'
          )

          replace_once(
              'index.html',
              '''            const [despesasSnap, receitasSnap, contasBancariasSnap] = await Promise.all([
                  getDocs(despesasQuery),
                  getDocs(receitasQuery),
                  getDocs(contasBancariasQuery)
              ]);

              const allDespesas = despesasSnap.docs;
              const allReceitas = receitasSnap.docs;
              const allContasBancarias = contasBancariasSnap.docs;
''',
              '''            const useCachedDespesas = despesasSnapshotLoaded;
              const useCachedReceitas = receitasSnapshotLoaded;
              const [despesasSnap, receitasSnap, contasBancariasSnap] = await Promise.all([
                  useCachedDespesas ? Promise.resolve(null) : getDocs(despesasQuery),
                  useCachedReceitas ? Promise.resolve(null) : getDocs(receitasQuery),
                  getDocs(contasBancariasQuery)
              ]);

              const despesasParaDashboard = useCachedDespesas ? allDespesas : despesasSnap.docs;
              const receitasParaDashboard = useCachedReceitas ? allReceitas : receitasSnap.docs;
              const allContasBancarias = contasBancariasSnap.docs;
''',
              'reuse cached top-level financial snapshots'
          )

          replace_exact_count(
              'index.html',
              '''            const pagamentosPromises = allDespesas.map(doc => getDocs(collection(doc.ref, 'pagamentos')));
''',
              '''            const pagamentosPromises = despesasParaDashboard.map(doc => {
                  const data = doc.data();
                  const hasPaymentHistory = (data.totalPago || 0) > 0 || ['Pago', 'Pago Parcialmente'].includes(data.status);
                  return hasPaymentHistory ? getDocs(collection(doc.ref, 'pagamentos')) : Promise.resolve(null);
              });
''',
              1,
              'skip payment history reads for titles without payments'
          )
          replace_exact_count(
              'index.html',
              '''            allDespesas.forEach((doc, index) => {
''',
              '''            despesasParaDashboard.forEach((doc, index) => {
''',
              1,
              'use cached payables in dashboard'
          )
          replace_exact_count(
              'index.html',
              '''                pagamentosSnapshots[index].forEach(pagoDoc => {
''',
              '''                pagamentosSnapshots[index]?.forEach(pagoDoc => {
''',
              1,
              'handle skipped payment history reads'
          )

          replace_exact_count(
              'index.html',
              '''            const recebimentosPromises = allReceitas.map(doc => getDocs(collection(doc.ref, 'recebimentos')));
''',
              '''            const recebimentosPromises = receitasParaDashboard.map(doc => {
                  const data = doc.data();
                  const hasReceiptHistory = (data.totalRecebido || 0) > 0 || ['Recebido', 'Recebido Parcialmente'].includes(data.status);
                  return hasReceiptHistory ? getDocs(collection(doc.ref, 'recebimentos')) : Promise.resolve(null);
              });
''',
              1,
              'skip receipt history reads for titles without receipts'
          )
          replace_exact_count(
              'index.html',
              '''            allReceitas.forEach((doc, index) => {
''',
              '''            receitasParaDashboard.forEach((doc, index) => {
''',
              1,
              'use cached receivables in dashboard'
          )
          replace_exact_count(
              'index.html',
              '''                recebimentosSnapshots[index].forEach(recebidoDoc => {
''',
              '''                recebimentosSnapshots[index]?.forEach(recebidoDoc => {
''',
              1,
              'handle skipped receipt history reads'
          )

          replace_once(
              'index.html',
              '''    function detachListeners() {
              if (notificationsUnsubscribe) notificationsUnsubscribe();
''',
              '''    function detachListeners() {
              if (dashboardRefreshTimer) {
                  clearTimeout(dashboardRefreshTimer);
                  dashboardRefreshTimer = null;
              }
              despesasSnapshotLoaded = false;
              receitasSnapshotLoaded = false;
              allDespesas = [];
              allReceitas = [];

              if (notificationsUnsubscribe) notificationsUnsubscribe();
''',
              'reset dashboard caches on listener teardown'
          )

          # Static safety assertions.
          fluxo = Path('fluxo-de-caixa.js').read_text(encoding='utf-8')
          mov = Path('movimentacao-bancaria.js').read_text(encoding='utf-8')
          notif = Path('notifications.js').read_text(encoding='utf-8')
          index = Path('index.html').read_text(encoding='utf-8')

          assert 'cumulativeSimulado += changes.simulado;' in fluxo
          assert 'runningSaldoSimulado = runningSaldoProjetado + changes.simulado;' not in fluxo
          assert 'usuarioConciliacao: conciliar ? currentUserName : null' in mov
          assert 'where("status", "==", "Vencido")' not in notif
          assert "collection(db, 'users', userId, 'usuarios')" not in index
          assert "collection(db, 'users', userId, 'systemUsers')" in index
          assert 'despesasParaDashboard' in index and 'receitasParaDashboard' in index
          print('All guarded patch assertions passed.')
          PY

      - name: Check JavaScript syntax
        run: |
          node --check fluxo-de-caixa.js
          node --check movimentacao-bancaria.js
          node --check notifications.js

      - name: Validate What-If accumulation behavior
        run: |
          node <<'NODE'
          const dailyChanges = {
            '2026-01-01': { realizado: 0, projetado: 0, simulado: -100000, comparado: -50000 },
            '2026-01-02': { realizado: 0, projetado: 200000, simulado: 0, comparado: 0 }
          };
          let runningSaldoRealizado = 1000000;
          let runningSaldoProjetado = 1000000;
          let cumulativeSimulado = 0;
          let cumulativeComparado = 0;
          const simulado = [];
          const comparado = [];
          for (const day of Object.keys(dailyChanges).sort()) {
            const changes = dailyChanges[day];
            runningSaldoRealizado += changes.realizado;
            runningSaldoProjetado += changes.realizado + changes.projetado;
            cumulativeSimulado += changes.simulado;
            cumulativeComparado += changes.comparado;
            const scenarioBase = runningSaldoProjetado;
            simulado.push(scenarioBase + cumulativeSimulado);
            comparado.push(scenarioBase + cumulativeComparado);
          }
          if (simulado[0] !== 900000 || simulado[1] !== 1100000) throw new Error(`Unexpected simulated balances: ${simulado}`);
          if (comparado[0] !== 950000 || comparado[1] !== 1150000) throw new Error(`Unexpected comparison balances: ${comparado}`);
          console.log('What-If accumulation test passed.');
          NODE

      - name: Commit patched files and remove temporary workflow
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/apply-erp-fixes.yml
          git add index.html fluxo-de-caixa.js movimentacao-bancaria.js notifications.js
          git commit -m "fix: financial consistency and Firestore query efficiency"
          git push origin HEAD:${GITHUB_REF_NAME}
